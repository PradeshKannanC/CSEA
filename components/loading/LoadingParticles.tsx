"use client";

import React from 'react';

/**
 * Section 8: Particle field completely removed to ensure clean, precise,
 * institutional motion with zero random dots, bubbles, or sparkles.
 */
export const LoadingParticles: React.FC<{ reducedMotion?: boolean }> = () => {
  return null;
};
